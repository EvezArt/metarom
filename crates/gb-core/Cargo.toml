[package]
name = "gb-core"
version = "0.3.0"
edition = "2021"
description = "MetaROM Game Boy emulator core — Phase 3: PPU + APU + Timer + MBC + CB decode"

[[bin]]
name = "letsplay"
path = "src/bin/letsplay.rs"

[lib]
name = "gb_core"
path = "src/lib.rs"

[dependencies]
